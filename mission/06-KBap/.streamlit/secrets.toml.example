# 이 파일을 같은 폴더에 secrets.toml 로 복사한 뒤 실제 키를 넣으세요.
# secrets.toml 은 절대 커밋하지 마세요 (.gitignore 의 secrets.* 로 차단됨).
#
# 배포 시에는 이 파일을 올리지 말고,
# Streamlit Cloud 대시보드 > App settings > Secrets 에 아래 내용을 동일하게 입력하세요.

OPENAI_API_KEY = "sk-여기에-실제-키"
